#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char msj1[] = "with proper grammar, but the outcome is a piece of art,\n";
printf("%s", msj1);
return (0);
}
